lists = ["水", "金", "地", "火", "木", "土", "天", "海", "冥"]

# for
for list in lists:
    print(list)

# while
index = 0
length = len(lists)
while index < length:
    print(lists[index])
    index = index + 1
